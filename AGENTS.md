# Agents

This repo's agent instructions live in [CLAUDE.md](./CLAUDE.md). Read that first.

It carries the non-negotiable rules, the reading order for a given job, and a
lookup table for finding the right file. Nothing here duplicates it.
